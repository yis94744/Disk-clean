# 01 - 全功能实测报告

- 日期：2026-08-30
- 环境：Windows 11 专业版 26200 x64 / Python 3.9 / PySide6 / 16GB RAM
- 被测版本：当前工作区代码（已含前几轮修复：二次元主题、系统信息进度条+温度/硬盘健康、回收站删除、卸载线程化等）
- 测试方式：GUI 实测（AX 自动化驱动按钮/下拉/键盘）+ 离屏 harness（驱动真实页面代码与真实副作用）+ 引擎级脚本测试
- 测试夹具：HKCR\Applications\DCFixtureTest 哑无效路径、HKCU Run\DCFixtureStartup 哑启动项、
  %TEMP%\DCDupFixture 重复文件组（含同大小不同内容对照组）、%TEMP%\DCBigFixture.bin 6MB 大文件、
  `subst X:` 虚拟盘（指向 %TEMP%，使全盘扫描类测试秒级完成）

## 一、逐页实测结果

| # | 页面/功能 | 用例 | 结果 | 备注 |
|---|-----------|------|------|------|
| 1 | 仪表盘 | 加载磁盘卡片（识别出 X: 虚拟盘，4 磁盘）| ✅ | |
| 1 | 仪表盘 | 快捷按钮「智能清理」跳转+自动分析 | ✅ | 前轮已验证，「扫描所有磁盘」同链路 |
| 2 | 安全清理 | 分析可清理项（Prefetch 修复后可扫出 9.30MB/227 文件）| ✅ | |
| 2 | 安全清理 | clean 引擎（剪贴板历史/DNS 缓存两项无害项）| ✅ | success，0 errors |
| 2 | 安全清理 | 扫描注册表 | ❌ | **永久卡在"正在扫描无效路径..."** → 问题 P1 |
| 2 | 安全清理 | 注册表清理执行（页面代码路径）| ✅ | harness A1：真实删除 HKCU 哑值 |
| 3 | 软件管理 | 393 个应用加载、图标、按大小排序、系统/用户着色 | ✅ | 详情面板为鼠标 itemClicked 信号，AX 无法触发（P10）|
| 4 | 重复文件 | X: 盘扫描，不同修改时间的重复文件检出 | ✅ | doc_copy1/copy2 被检出为 1 组 |
| 4 | 重复文件 | 同大小不同内容不误报 | ✅ | same_size_diff.txt 未被报告 |
| 4 | 重复文件 | 删除：保留最优路径 + 进回收站 | ✅ | harness C1：保留 doc_copy1，copy2 进回收站 |
| 5 | 大文件 | X: 盘扫描（最小 1MB），17 个文件，夹具 DCBigFixture.bin 检出 | ✅ | 类别显示英文 "System" → P4 |
| 6 | 文件可视化 | X: 盘扫描，6953 文件/3.37GB，安全列有评分、所属软件归因 | ✅ | 安全标签英文 → P5 |
| 7 | 启动管理 | 列表加载 | ⚠️ | HKCU 启动项全部缺失 → P2；desktop.ini 混入 → P3 |
| 7 | 启动管理 | 真禁用（StartupApproved 写入 0x03）| ✅ | harness B1（用正确 hive 数据驱动页面代码）|
| 8 | 进程管理 | 277 进程/91 组加载、中文归属、未选中防呆提示 | ✅ | 加载约 15s → P8 |
| 8 | 进程管理 | 结束进程执行 | ✅ | harness D2：真实 pythonw 被终止 |
| 9 | 系统信息 | 进度条 + 温度 + 硬盘健康 | ✅ | 前轮已验证 |
| 10 | 设置 | 恢复默认（不崩溃）、auto_start 开关 | ✅ | 引擎级验证注册表写入/删除 |
| 11 | 磁盘分析器（treemap） | 可达性 | ❌ | **不在侧边栏导航中，功能不可达** → P6 |
| 12 | 一键清理（全量） | — | ⏭️ 未执行 | 会真实清理 Edge 缓存等；clean 阻塞 UI 线程问题保留为 P9 |

## 二、harness 补充验证（绕过 AX 勾选/选择限制，驱动真实页面代码）

| 用例 | 结果 |
|------|------|
| A1 安全清理._clean_reg 真实删除注册表值 | PASS |
| B1 启动管理._disable_selected 写入 StartupApproved | PASS |
| C1 重复文件._delete_selected 保留最优+回收站 | PASS |
| D2 进程管理._kill 真实结束进程 | PASS |
| E1 软件管理卸载取消路径无副作用 | PASS |

合计 6/6 通过。

## 三、测试期间的应用级异常

- 会话中出现过一次"窗口最小化 + 状态栏文本串扰（显示其它页面的状态消息）"，重启应用后未复现，
  且无日志证据，判定为 AX 观测干扰而非应用缺陷（P11，仅记录不修复）。

## 四、结论

核心功能链路（扫描、清理项分析、重复文件、大文件、软件管理、进程管理、系统信息、设置）
全部可用且此前的修复均验证生效。**注册表扫描因 P1 回归完全不可用**（最高优先级），
启动管理因 P2 缺失全部 HKCU 启动项。详细复现步骤见 `02-问题复现文档.md`，
修复方案与审计见 `03/04` 文档。

---

## 五、修复实施与验证结果（回填）

| 项 | 验证方式 | 结果 |
|----|----------|------|
| F1/F2 注册表扫描崩溃+线程兜底 | 离线 run() 0.0s 完成、24 项检出、夹具命中；注入异常时 UI 收到错误提示且 finished 正常发出；GUI 扫描 20s 完成不卡死 | ✅ |
| F3 HKCU hive 修正 | GUI 启动管理显示 HKCU 全部 3 项（ctfmon/OneDrive/Edge），"已禁用"状态与系统真实状态一致 | ✅ |
| F4 desktop.ini 过滤 | GUI 列表不再出现 desktop.ini | ✅ |
| F5 类别中文映射 | .bin → "系统驱动" | ✅ |
| F6 安全标签中文 | GUI 显示"安全/建议清理/谨慎/受保护" | ✅ |
| F7 磁盘分析页入导航 | 侧边栏出现"磁盘分析"，扫描 X: 后 TreeMap + 文件列表完整渲染 | ✅ |
| F8 一键清理线程化 | harness：清理期间按钮禁用 → 完成恢复，释放量汇总正确 | ✅ |
| F9 @MUI/资源索引/%env% 误报 | 9/9 边界用例；全量扫描 24 → 10 条且剩余均为真实残留 | ✅ |

### 测试副作用披露
- 离屏 harness 验证 F8 时，页面中默认勾选的全部 SAFE 级清理项被真实执行了一次清理
  （临时文件/浏览器缓存等，共释放 4.01 MB，3 项因文件被占用跳过）；**回收站被一并清空**
  （当时回收站内只有本测试删入的夹具文件）。系统缓存均属可再生数据，无用户文档受影响。
- 全部注册表/文件系统夹具已清理并复核无残留；X: 虚拟盘已卸载。
